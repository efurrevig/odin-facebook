<div data-controller="toggle" class="min-vh-100 d-flex align-items-center">
  <div class="login container shadow p-3 mb-5 rounded">
    <!-- Sign-in, Register toggle -->
    <ul class="nav nav-pills nav-justified mb-3" id="ex1" role="tablist">
      <li class="nav-item" role="presentation">
        <button data-action="click->toggle#displayLogin" data-toggle-target="loginButton" class="nav-link active" id="tab-login">Login</button>
      </li>
      <li class="nav-item" role="presentation">
        <button data-action="click->toggle#displayRegister" data-toggle-target="registerButton" class="nav-link" id="tab-register">Register</button>
      </li>
    </ul>
    <!-- OmniAuth Links -->
    <div data-toggle-target="login">
      <%= render "login" %>
    </div>
    <div data-toggle-target="register" class="hidden">
      <%= render "register" %>
    </div>
  </div>
<div>

<!-- 
  Select buttion ->
    Display relative form
-->